# Lab-2-Web-development
 the fundamentals of HTTP communication by inspecting request and response cycles using browser DevTools and Postman. Network requests were analyzed to observe headers, status codes, and request methods. GET and POST requests were executed, and caching behavior along with different HTTP status codes (2xx, 3xx, 4xx) use static page.
